#include <string>
#include <fstream>
#include "digraph.h"
/*
using namespace std;

class GraphVisualizer {
public:
    GraphVisualizer();
    ~GraphVisualizer();
    virtual void visualize(DiGraph &graph, std::string, std::string , std::string, List<Edge*> e);
};

class DotGraphVisualizer : GraphVisualizer {
public:
    void visualize(DiGraph &graph, std::string);
    DotGraphVisualizer(void);
    ~DotGraphVisualizer(void);

};

*/